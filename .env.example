# Vite / client — optional: assign each blind user a MongoDB ObjectId (24 hex chars).
# If unset, the app generates one and keeps it in localStorage.
# VITE_PERSON_USER_ID=69bebe4b780b22d0c3e43d57

# If API is on another host (production):
# VITE_API_BASE=https://your-api.example.com
